# Module 3 Practice Questions

1. What is the difference between `if` and `switch`?
2. When is `else if` useful?
3. When is a `for` loop better than a `while` loop?
4. What is the difference between `while` and `do-while`?
5. What does `break` do?
6. What does `continue` do?
7. Why should you be careful to avoid infinite loops?
8. What is the purpose of an assertion?
9. Why should assertions not replace normal user input validation?
10. Why is it important to test different values in control-flow programs?