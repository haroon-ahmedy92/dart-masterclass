# Module 2 Practice Questions

1. What is the difference between `var` and an explicit type like `String`?
2. What is type inference?
3. What is the difference between `dynamic` and `var`?
4. What does sound null safety help prevent?
5. What is the difference between `final` and `const`?
6. What is the purpose of the `?` symbol in a type like `String?`
7. What is the difference between `=` and `==`?
8. What does `~/` do?
9. Why do we import `dart:io` for terminal input?
10. Why is `stdin.readLineSync()` often followed by `??`?